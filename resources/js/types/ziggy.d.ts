/**
 * Deklarasi global untuk fungsi route() dari Ziggy.
 * Ini memberitahu TypeScript bahwa fungsi 'route' ada secara global
 * dan bisa digunakan di seluruh proyek tanpa perlu diimpor.
 */
declare function route(name?: string, params?: any, absolute?: boolean): string;
